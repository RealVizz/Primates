/**
 * Because we know ahead of runtime what we can have, so this enum represents all the possible
 * values of the favorite food an animal can have.
 */
public enum FavoriteFood {
  NULL, EGG, FRUIT, INSECT, LEAVES, NUTS, SEEDS, TREE_SAP
}
